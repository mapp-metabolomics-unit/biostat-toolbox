# To contribute
You will need `uv` installed. You can install it using `curl -LsSf https://astral.sh/uv/install.sh | sh` on Linux of MacOS and 
`powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"` on Windows.